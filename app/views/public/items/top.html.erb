<section class="hero">
  <div class="title-frame"><h1 class="title">Welcome to Nagano Cake</h1></div>
  <br>
  <div><p>
  <div class="text">ECサイトがオープンいたしました！<br>
	                  たくさんのお菓子達があなたを待っています。<br>
	                  <br>
	                  ※購入・配送には会員登録が必要になります。<br>
	</p></div>
<section>

  <h4 class="new">NEW　ITEMs</h4>
 <div class="top-new">
 <% @items.each do |item| %>
 <table class= "d-inline-block">
 <tr>

 <td class="image-top-new">
              <% if admin_signed_in? %>
                <%= link_to admin_item_path(item) do %>
                  <%= attachment_image_tag(item, :image, format: 'jpeg', size: "200x150") %>
                <% end %>
              <% else %>
                <%= link_to item_path(item) do %>
                  <%= attachment_image_tag(item, :image, format: 'jpeg', size: "200x150") %>
                <% end %>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><%= item.name %></td>
          </tr>

        </table>
        <% end %>
        <p class="text-md-center pb-3 background-color:#EFEFEF">
          <%= link_to "︎see more...", items_path, class: ' btn btn-outline-light btn-sm ' %>
        </p>
      </div>

    </div>
  </div>
</div>
