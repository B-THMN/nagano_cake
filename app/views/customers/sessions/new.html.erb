<div class="container row mx-auto">
  <div class="signin text-center py-5 px-3 col-md-6 mx-auto rounded">
    <h3 class="signin-title mb-5">ログイン</h3>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <h5><span style="background-color:#F5F5F5">　会員の方はこちらからログイン　</h5>
    <br>
    <div class="field">
      <%= f.label "メールアドレス" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'sample@example.com' %>
    </div>

    <div class="field">
      <%= f.label "パスワード" %>
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div><br>
    <div class="actions">
      <%= f.submit "ログイン", class: 'btn btn-outline-primary' %>
    </div>
  <% end %>
<br>
  <h5><span style="background-color:#F5F5F5">　会員登録がお済みでない方　</h5>
  <table>
    <tr>
      <td><%= render "customers/shared/links" %></td>
      <td>から新規登録を行ってください。</td>
    </tr>
  </table>

</div>